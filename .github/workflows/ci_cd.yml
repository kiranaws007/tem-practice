name: wWisecow CI/CD Pipeline

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2
    
    - name: Login to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_HUB_USERNAME }}
        password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}
    
    - name: Build and push Docker image
      uses: docker/build-push-action@v2
      with:
        context: .
        push: true
        tags: kirankumars/wisecowapp:latest    
  deploy:
    runs-on: ubuntu-latest
    needs: build
    steps:
    - name: Checkout code
      uses: actions/checkout@v2
    
    - name: Set up kubectl
      uses: actions-hub/kubectl@master
      with:
        version: '1.21.0'
        config: ${{ secrets.KUBECONFIG }}
    
    - name: Create TLS secret
      run: |
        kubectl create secret tls wisecow-tls --cert=Problem Statement -1/tls.crt --key=Problem Statement -1/tls.key --dry-run=client -o yaml | kubectl apply -f -
    
    - name: Deploy to Kubernetes
      run: |
        kubectl apply -f Problem Statement -1/deployment.yaml
        kubectl apply -f Problem Statement -1/service.yml
        kubectl apply -f Problem Statement -1/ingress.yml
